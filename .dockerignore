.venv
__pycache__
*.pyc
.git
.gitignore
.pytest_cache
.ipynb_checkpoints
data
notebook
*.csv
*.ipynb
tests

# Secrets / local-only config - never bake into the image.
.env

# MLflow local tracking store - large and mostly dev-only. The Dockerfile
# copies in only the model artifact directory the API actually serves.
mlflow.db
mlflow_flight_delay.db
mlruns
